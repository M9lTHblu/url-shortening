import styled from 'styled-components/macro'

const InputError = styled.span`
  position: absolute;
  left: 10px;
  bottom: -18px;
  font-size: 13px;
  color: var(--danger);
`

export default InputError
